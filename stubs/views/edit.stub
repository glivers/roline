@extends('{{ResourceName|lowercase}}/layout')

@section('title', 'Edit {{ResourceName}}')

@section('content')
    <div class="page-header">
        <h1>Edit {{ResourceName}}</h1>
        <a href="{{ Url::link('{{ResourceName|lowercase}}') }}" class="btn">Back to List</a>
    </div>

    <div class="card">
        <form method="POST" action="{{ Url::link('{{ResourceName|lowercase}}/update/' . $id) }}" class="form">
            {{{ CSRF::field() }}}

            <div class="form-group">
                <label for="title">Title <span class="required">*</span></label>
                <input
                    type="text"
                    id="title"
                    name="title"
                    value="{{ ${{ResourceName|lowercase}}['title'] or '' }}"
                    required
                    class="form-control"
                    placeholder="Enter task title"
                >
            </div>

            <div class="form-group">
                <label for="description">Description</label>
                <textarea
                    id="description"
                    name="description"
                    rows="5"
                    class="form-control"
                    placeholder="Add more details about this task (optional)"
                >{{ ${{ResourceName|lowercase}}['description'] or '' }}</textarea>
            </div>

            <div class="form-row">
                <div class="form-group">
                    <label for="status">Status <span class="required">*</span></label>
                    <select id="status" name="status" class="form-control" required>
                        <option value="pending" @if(${{ResourceName|lowercase}}['status'] == 'pending') selected @endif>Pending</option>
                        <option value="completed" @if(${{ResourceName|lowercase}}['status'] == 'completed') selected @endif>Completed</option>
                    </select>
                </div>

                <div class="form-group">
                    <label for="priority">Priority <span class="required">*</span></label>
                    <select id="priority" name="priority" class="form-control" required>
                        <option value="low" @if(${{ResourceName|lowercase}}['priority'] == 'low') selected @endif>Low</option>
                        <option value="medium" @if(${{ResourceName|lowercase}}['priority'] == 'medium') selected @endif>Medium</option>
                        <option value="high" @if(${{ResourceName|lowercase}}['priority'] == 'high') selected @endif>High</option>
                    </select>
                </div>
            </div>

            <div class="form-actions">
                <button type="submit" class="btn btn-primary">Update</button>
                <a href="{{ Url::link('{{ResourceName|lowercase}}/show/' . $id) }}" class="btn btn-secondary">Cancel</a>
            </div>
        </form>
    </div>
@endsection
